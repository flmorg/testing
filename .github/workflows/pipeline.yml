name: Pipeline

on:
  push:
    tags:
      - "v*.*.*"
  # pull_request:
  #   paths:
  #     - 'code/**'

jobs:
  build:
    uses: flmorg/testing/.github/workflows/build.yml@main
    secrets: inherit